import MessagesInbox from '../../components/messages/MessagesInbox';

export default function DesignerMessagesPage() {
  return (
    <MessagesInbox
      heading="Messages"
      description="Mailbox for admin communications, ticket notices, and platform messages."
    />
  );
}
