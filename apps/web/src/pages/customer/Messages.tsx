import MessagesInbox from '../../components/messages/MessagesInbox';

export default function CustomerMessagesPage() {
  return (
    <MessagesInbox
      heading="Messages"
      description="Mailbox for order updates, support communications, and platform announcements."
    />
  );
}
